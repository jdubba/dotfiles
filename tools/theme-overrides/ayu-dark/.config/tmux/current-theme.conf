# ayu-dark tmux colors
set -g pane-border-style fg=#686868
set -g pane-active-border-style fg=#fae994
# status-style, not the deprecated status-bg/status-fg pair: on tmux 3.5a those
# do not feed what renders, leaving the default bg=green,fg=black in place.
# Clear the deprecated pair first: it takes precedence over status-style at
# render time, and lives in the server, so a stale value from an older seam
# would override this forever.
set -gu status-bg
set -gu status-fg
set -g status-style 'bg=#304357,fg=#b3b1ad'
set -g status-left '#[fg=#90e1c6]#S #[fg=#f9af4f]|'
set -g status-right '#[fg=#90e1c6]%Y-%m-%d #[fg=#b3b1ad]%H:%M #[fg=#f9af4f][#(whoami)]'
setw -g window-status-format '#I:#W'
setw -g window-status-current-format '#[fg=#c7c7c7,bold]#I:#W#[default]'
