# fedora hyprlock auth include (sourced from the shared ~/.config/hypr/
# hyprlock.conf via `source = hyprlock-local.conf`).
#
# Per-host hyprlock authentication seam. This host has no confirmed enrolled
# fingerprint sensor, so nothing is enabled here and hyprlock uses its built-in
# PAM/password default. Intentionally a comment-only stub so the shared
# `source = hyprlock-local.conf` resolves on this machine.
